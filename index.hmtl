<!doctype html>
<html lang="id">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Mengenal Sedikit tentang Naila</title>
  <style>
    :root{
      --bg:#f6f8fb;
      --card:#ffffff;
      --accent:#5b8def;
      --text:#0f1724;
      --muted:#55606a;
      --radius:12px;
      --maxw:760px;
      --pad:24px;
      font-family: "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
    }
    body{
      margin:0;
      background:linear-gradient(180deg, #eef4ff 0%, var(--bg) 100%);
      color:var(--text);
      display:flex;
      align-items:center;
      justify-content:center;
      min-height:100vh;
      padding:40px 16px;
    }
    .card{
      width:100%;
      max-width:var(--maxw);
      background:var(--card);
      border-radius:var(--radius);
      box-shadow:0 8px 30px rgba(18,38,63,0.08);
      padding:var(--pad);
      box-sizing:border-box;
      border:1px solid rgba(15,23,36,0.04);
    }
    header{
      display:flex;
      gap:16px;
      align-items:center;
      margin-bottom:12px;
    }
    .avatar{
      width:72px;
      height:72px;
      border-radius:14px;
      background:linear-gradient(135deg,#dbe9ff,#cfe7ff);
      display:flex;
      align-items:center;
      justify-content:center;
      font-weight:700;
      color:var(--accent);
      font-size:20px;
      flex:0 0 72px;
    }
    h1{
      margin:0;
      font-size:18px;
      letter-spacing:0.2px;
    }
    p.lead{
      margin:6px 0 0 0;
      colo
